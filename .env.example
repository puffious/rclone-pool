# Example environment variables for Docker Compose

# Timezone (optional)
TZ=UTC

# Rclone configuration file path
RCLONE_CONFIG=/config/rclone.conf

# rclonepool configuration file path
RCLONEPOOL_CONFIG=/config/config.json

# WebDAV server port (must match config.json)
WEBDAV_PORT=8080

# Optional: Enable debug logging
# LOGLEVEL=DEBUG
